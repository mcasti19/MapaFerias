# Force pnpm to hoist all @types packages into node_modules/@types
# so the Windows VS Code TypeScript server can resolve them
public-hoist-pattern[]=*types*
public-hoist-pattern[]=*eslint*
public-hoist-pattern[]=@vitejs/*
